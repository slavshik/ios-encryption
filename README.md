To run example generate keys(key.pub and key) somewhere:
	ssh-keygen
Add generated keys into XCode project navigator:
!(screen1.png)
!(screen2.png  "In add to targets section \"EncryptTest\" should be checked, the rest is your choice.")